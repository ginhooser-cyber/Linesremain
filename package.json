{
  "name": "lineremain",
  "version": "1.0.0",
  "description": "Lineremain — A web-based multiplayer survival game",
  "private": true,
  "workspaces": [
    "shared",
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"npm run dev -w client\" \"npm run dev -w server\"",
    "build": "npm run build -w shared && npm run build -w client && npm run build -w server",
    "build:shared": "npm run build -w shared",
    "start": "npm run start -w server",
    "db:generate": "npm run db:generate -w server",
    "db:migrate": "npm run db:migrate -w server",
    "db:seed": "npm run db:seed -w server",
    "lint": "eslint .",
    "format": "prettier --write .",
    "test": "vitest run",
    "clean": "rm -rf node_modules client/node_modules server/node_modules shared/node_modules client/dist server/dist shared/dist"
  },
  "keywords": [
    "game",
    "survival",
    "multiplayer",
    "voxel",
    "stickman"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "concurrently": "^9.2.1",
    "eslint": "^9.39.2",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.54.0",
    "vitest": "^4.0.18"
  }
}
